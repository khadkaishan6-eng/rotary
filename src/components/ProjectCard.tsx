import Link from 'next/link';
import { ArrowUpRight, CalendarDays, MapPin } from 'lucide-react';

type Project = { id: string; title: string; summary: string; date: string; status: string; avenue: string; location: string; image: string };
export function ProjectCard({ project }: { project: Project }) {
  return <article className="group overflow-hidden rounded-2xl border border-slate-200 bg-white transition hover:-translate-y-1 hover:shadow-xl hover:shadow-[#17458f]/10"><div className="h-52 overflow-hidden bg-slate-100"><img src={project.image} alt={`Scene representing ${project.title}`} className="h-full w-full object-cover transition duration-500 group-hover:scale-105" /></div><div className="p-5"><div className="flex items-center justify-between gap-3"><span className="rounded-full bg-[#eaf6fb] px-3 py-1 text-xs font-bold text-[#17458F]">{project.avenue}</span><span className={`text-xs font-bold capitalize ${project.status === 'completed' ? 'text-slate-400' : 'text-[#00A2E0]'}`}>{project.status}</span></div><h3 className="mt-4 font-display text-xl text-[#17458F]">{project.title}</h3><p className="mt-2 text-sm leading-6 text-slate-600">{project.summary}</p><div className="mt-5 flex items-center gap-4 border-t border-slate-100 pt-4 text-xs text-slate-500"><span className="flex items-center gap-1"><CalendarDays size={14} /> {new Date(project.date).toLocaleDateString('en-US', { month: 'short', year: 'numeric' })}</span><span className="flex items-center gap-1"><MapPin size={14} /> {project.location}</span><Link aria-label={`View ${project.title}`} href={`/projects/${project.id}`} className="ml-auto text-[#17458F] hover:text-[#F7A81B]"><ArrowUpRight size={18} /></Link></div></div></article>;
}
