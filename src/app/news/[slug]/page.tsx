import Link from 'next/link';
import { ArrowLeft, Copy, MessageCircle } from 'lucide-react';
import { FaFacebookF, FaInstagram } from 'react-icons/fa';
import { notFound } from 'next/navigation';
import { news } from '@/data/site';

export function generateStaticParams() { return news.map((article) => ({ slug: article.slug })); }
export default async function ArticlePage({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params;
  const article = news.find((item) => item.slug === slug);
  if (!article) notFound();
  const related = news.filter((item) => item.slug !== slug).slice(0, 2);
  return <><section className="hero-banner"><div className="container-shell py-12"><Link href="/news" className="inline-flex items-center gap-2 text-sm font-bold text-blue-100 hover:text-[#F7A81B]"><ArrowLeft size={16} /> Back to News & Articles</Link><p className="mt-8 text-xs font-bold uppercase tracking-[0.14em] text-[#F7A81B]">{article.type} · {article.readTime}</p><h1 className="mt-4 max-w-4xl font-display leading-tight">{article.title}</h1><p className="mt-4 text-sm text-blue-100">By {article.author} · {new Date(article.date).toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' })}</p></div></section><article className="container-shell max-w-4xl py-16 md:py-20"><img src={article.image} alt="" className="aspect-[16/8] w-full rounded-xl object-cover" /><div className="mt-10 space-y-5 text-base leading-8 text-slate-600"><p>This placeholder story begins with a question communities ask every day: what would make the next season a little more hopeful? For our club, the answer starts with making time to listen.</p><p>Members and partners gathered around a shared intention, bringing different experiences and practical ideas to the same table. The work is intentionally local, shaped by people who know the context best.</p><p>That approach changes the pace of service. We measure progress in useful conversations, new confidence and relationships that keep going after an event ends.</p><p>As this placeholder initiative develops, RCK will continue to share what we learn, where support is needed and how members can participate with care.</p><p>Service with soul is not a slogan detached from the work. It is a daily choice to be present, accountable and generous with our attention.</p></div><div className="mt-10 rounded-xl bg-[#F5F7FA] p-6"><p className="text-xs font-bold uppercase tracking-[0.12em] text-[#17458F]">About the author</p><p className="mt-2 font-display text-xl text-[#0A2350]">{article.author}</p><p className="mt-2 text-sm leading-6 text-slate-600">A clearly labelled placeholder author profile for this article.</p></div><div className="mt-7 flex flex-wrap items-center gap-3 border-t border-slate-200 pt-6"><span className="text-sm font-bold text-[#0A2350]">Share</span><button aria-label="Share on Facebook" className="grid h-9 w-9 place-items-center rounded-full border border-slate-200 text-[#17458F]"><FaFacebookF size={16} /></button><button aria-label="Share on WhatsApp" className="grid h-9 w-9 place-items-center rounded-full border border-slate-200 text-[#17458F]"><MessageCircle size={16} /></button><button aria-label="Share on Instagram" className="grid h-9 w-9 place-items-center rounded-full border border-slate-200 text-[#17458F]"><FaInstagram size={16} /></button><button aria-label="Copy article link" className="grid h-9 w-9 place-items-center rounded-full border border-slate-200 text-[#17458F]"><Copy size={16} /></button></div></article><section className="bg-[#F5F7FA] py-16"><div className="container-shell"><h2 className="font-display text-2xl text-[#17458F]">More to read</h2><div className="mt-7 grid gap-8 md:grid-cols-2">{related.map((item) => <Link key={item.id} href={`/news/${item.slug}`} className="rounded-xl bg-white p-5"><p className="text-xs font-bold uppercase tracking-[0.12em] text-[#17458F]">{item.type}</p><h3 className="mt-3 font-display text-xl text-[#0A2350]">{item.title}</h3><p className="mt-2 text-sm text-slate-600">{item.excerpt}</p></Link>)}</div></div></section></>;
}
